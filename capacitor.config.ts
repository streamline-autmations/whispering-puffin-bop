import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.whisperingpuffinbop',
  appName: 'whispering-puffin-bop',
  webDir: 'dist'
};

export default config;
